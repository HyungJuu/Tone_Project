<UserControl x:Class="LoginApp.Views.SignInView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LoginApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Border BorderBrush="Black" BorderThickness="1" Background="LightGray" Width="300" Height="200">
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.ColumnSpan="3">
                <TextBox Grid.Row="0" Grid.ColumnSpan="3" Text="{Binding Id}" Margin="10 15 10 0" />
                <TextBox Grid.Row="1" Grid.ColumnSpan="3" Text="{Binding Password}" Margin="10 0 10 0" />
                <TextBlock Grid.Row="3" Text="{Binding LoginMessage}"
                           HorizontalAlignment="Center"
                           Foreground="Red" Margin="0 5" />
                <Button Grid.Row="2" Grid.ColumnSpan="3" Content="로그인"
                        Command="{Binding SignInCommand}" Style="{StaticResource SignInOut}" Margin="0 0 0 10" />
            </StackPanel>

            <Button Grid.Row="1" Grid.Column="0" Content="회원가입" HorizontalAlignment="Left" Margin="10 0 0 0" />
            <Button Grid.Row="1" Grid.Column="1" Content="계정찾기"  HorizontalAlignment="Right"/>
            <Button Grid.Row="1" Grid.Column="2" Content="비밀번호 찾기" HorizontalAlignment="Right" Margin="10 0 10 0"/>
        </Grid>
    </Border>
</UserControl>
