<UserControl x:Class="LoginApp.Views.SnakeGame.SnakeGamePlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LoginApp.Views.SnakeGame"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsPaused}" Value="True">
                    <Setter Property="Content" Value="재개" />
                </DataTrigger>
                <DataTrigger Binding="{Binding IsPaused}" Value="False">
                    <Setter Property="Content" Value="일시정지" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 일시정지/재개 버튼 -->
        <Button Grid.Column="0" Command="{Binding TogglePauseCommand}" Width="80" Height="25" HorizontalAlignment="Right" Margin="0 10 70 0" />

        <!-- 10분 타이머 표시 -->
        <TextBlock Grid.Column="1" Text="{Binding TimerDisplay}" 
            
            HorizontalAlignment="Left" Margin="100 15 0 0" Height="30"
            FontSize="20" Foreground="Black" />

        <Grid Grid.Row="1" Grid.ColumnSpan="2">
            <!-- 게임 영역 -->
            <Border BorderBrush="MediumVioletRed" BorderThickness="3" Width="300" Height="330" Background="Black">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="게임기능 추가 예정" Foreground="White" FontSize="20" />
                </StackPanel>
            </Border>
            
            <!-- 카운트다운 표시 -->
            <TextBlock Text="{Binding CountdownDisplay}" 
           HorizontalAlignment="Center" VerticalAlignment="Center"
           FontSize="50" Foreground="Red" />
        </Grid>

        

        
    </Grid>
</UserControl>
